## Codebase Patterns
- Client uses baseUrl: "./src" in tsconfig.json for path aliases (import from "API", "hooks", "pages", etc.)
- @tailwindcss/vite ESM-only plugin fails with Vite 6 CJS mode — use @tailwindcss/postcss instead
- shadcn/ui path aliases use @/ prefix (e.g. @/components, @/lib) mapped in both tsconfig paths and vite resolve.alias
- Use `pnpm dlx shadcn@latest add <component> --yes` to add shadcn components
- Vite config mirrors these aliases via resolve.alias in vite.config.ts
- Environment variables use VITE_* prefix, accessed via import.meta.env.VITE_*
- Vite 6.x used (not 8.x) due to Node 20.10 on this machine; Vite 8 requires Node 20.19+
- Dev server runs on port 3000 with proxy to backend at localhost:5001
- Vite resolve aliases must be added for each src/ subdirectory (e.g. `stores: path.resolve(__dirname, "src/stores")`) — tsconfig baseUrl alone doesn't work for Vite
- Use `useXxxStore.getState()` for non-React contexts (API interceptors, refreshToken, event handlers inside useEffect); use `useXxxStore(state => state.xxx)` inside React components/hooks
- Zustand stores use `skipHydration: true` for encrypted persistence — call `setEncryptionDigest(digest)` then `store.persist.rehydrate()` after login
- Zustand stores are in src/stores/ (auth.ts, chats.ts, ui.ts); encrypted middleware in src/stores/middleware/encrypted-storage.ts
- The Dialog system uses UIStore `dialog` state with `{ Content, submitLabel, cancelLabel, onConfirm }` shape — rendered by src/components/Dialog.tsx using shadcn Dialog
- `DialogClose` exported from `components/Dialog` is still used by old Main page components (Settings, UsersReaction) — keep until those pages are redesigned
- Layout uses Tailwind responsive classes: `hidden md:flex` / `flex` to toggle sidebar vs chat panel on mobile; no CSS media queries needed
- Settings component renders its own trigger button — import it directly in SideHeader, don't try to extract the dialog content separately

# Ralph Progress Log
Started: Sun Mar 29 19:17:31 CEST 2026
---

## 2026-03-29 - US-001
- Migrated from CRA (react-app-rewired) to Vite
- Files changed:
  - Removed: config-overrides.js, src/react-app-env.d.ts, src/reportWebVitals.ts, src/setupTests.ts, public/index.html
  - Created: vite.config.ts, vite-env.d.ts, index.html (at app root)
  - Modified: package.json (scripts, deps), tsconfig.json (ES2022, bundler moduleResolution), src/API/index.ts, src/hooks/useSocket.ts (REACT_APP_* -> VITE_*), src/index.tsx (removed reportWebVitals), .gitignore
- Removed deps: react-app-rewired, react-scripts, react-error-overlay, buffer, process, stream-browserify
- Added deps: vite@6, @vitejs/plugin-react@4, vite-plugin-node-polyfills, typescript@5.7, @types/node@22
- **Learnings for future iterations:**
  - Vite 8 needs Node 20.19+ — this machine has 20.10, so Vite 6 was used instead
  - vite-plugin-node-polyfills handles Buffer/stream/process polyfills that config-overrides.js used to provide
  - The CJS deprecation warning from Vite is harmless and can be ignored
  - Typecheck passes, dev server starts on port 3000
---

## 2026-03-29 - US-002
- Set up Tailwind CSS v4 + shadcn/ui in the client app
- Files changed:
  - Created: src/styles/globals.css (Tailwind directives + shadcn CSS variables for light/dark), postcss.config.js, components.json, src/lib/utils.ts (cn() helper), src/components/ui/button.tsx (shadcn Button)
  - Modified: vite.config.ts (added @/ alias), tsconfig.json (added paths for @/*), src/index.tsx (import globals.css), package.json (new deps)
- Added deps: tailwindcss, @tailwindcss/postcss, class-variance-authority, clsx, tailwind-merge, lucide-react, radix-ui (via shadcn)
- **Learnings for future iterations:**
  - @tailwindcss/vite is ESM-only and doesn't work with Vite 6 CJS loading — must use @tailwindcss/postcss instead
  - Tailwind v4 uses `@import "tailwindcss"` instead of v3's `@tailwind base/components/utilities`
  - Dark mode uses `@custom-variant dark (&:is(.dark *))` in Tailwind v4
  - shadcn CLI (`pnpm dlx shadcn@latest add <component> --yes`) auto-installs component deps like radix-ui
  - Old styles (Bootstrap/SCSS) coexist fine alongside Tailwind — no conflicts
---

## 2026-03-29 - US-003
- Set up Zustand stores to replace Recoil atoms
- Files created:
  - src/stores/middleware/encrypted-storage.ts — AES encrypted localStorage using crypto-js, replicating atoms/effects/persist.ts but with a module-level digest variable instead of recoil-nexus getRecoil
  - src/stores/auth.ts — LoggedUser state with token update, settings update, and encrypted persistence (skipHydration: true)
  - src/stores/chats.ts — Chat records with message update, typing, delete methods, encrypted persistence (skipHydration: true)
  - src/stores/ui.ts — Combined theme, dialog, focus, timestamp, replyingTo, sessionTimeout (no persistence)
- **Learnings for future iterations:**
  - Zustand persist with `skipHydration: true` solves the bootstrap problem: stores start empty, then `hydrate()` is called after login when the digest is available
  - `setEncryptionDigest()` must be called before `hydrate()` and before any persistence writes — the auth store's `setUser` does this automatically
  - The encrypted-storage middleware uses a module-level `_digest` variable instead of recoil-nexus, making it framework-agnostic
  - Recoil and Zustand coexist without conflicts — no need to remove Recoil yet
---

## 2026-03-29 - US-004a
- Rewired API layer and auth hooks from Recoil to Zustand
- Files changed:
  - Modified: src/API/index.ts (getRecoil → useAuthStore.getState()), src/API/refreshToken.ts (getRecoil/setRecoil → useAuthStore.getState()), src/hooks/usePrivateKey.ts (useRecoilValue → useAuthStore), src/components/FocusHandler.tsx (Recoil atoms → useUIStore)
  - Modified: src/pages/Login/handlers/useHandleSubmit.tsx (Recoil → Zustand stores + encrypted persistence rehydration), src/pages/Login/handlers/useHandleRecovery.tsx (dialogState → useUIStore), src/pages/Login/handlers/useHandleMnemonicSubmit.tsx (3 Recoil atoms → 3 Zustand stores), src/pages/Login/handlers/useHandleRegenerate.tsx (3 Recoil atoms → 3 Zustand stores)
  - Modified: src/pages/Register/index.tsx (useSetRecoilState → useAuthStore + useUIStore)
  - Modified: src/stores/auth.ts (added updateTokens method for refreshToken flow)
  - Modified: vite.config.ts (added stores alias)
- **Learnings for future iterations:**
  - Vite resolve.alias must include `stores` directory — it was missing after US-003 created the stores
  - For non-React code (API interceptors, standalone functions), use `useAuthStore.getState()` instead of hooks
  - Login flow with Zustand: call `setEncryptionDigest(digest)` → `useAuthStore.persist.rehydrate()` → read hydrated state
  - `useHandleDigestUpdate` was already a pure function (no Recoil hooks), so it needed no changes
  - Recoil atoms and imports still exist but are no longer used by the API layer, auth hooks, login, register, or FocusHandler
---

## 2026-03-29 - US-004b
- Rewired Socket.IO and all message-related hooks from Recoil to Zustand
- Files changed:
  - Modified: src/hooks/useSocket.ts (userState → useAuthStore, chatsState → useChatsStore.getState() for typing/disconnect handlers)
  - Modified: src/hooks/useActiveChat.ts (chatsState/userState → useChatsStore/useAuthStore)
  - Modified: src/hooks/useUpdateMessage.ts (setChats Recoil → useChatsStore.updateMessage)
  - Modified: src/hooks/useDisplayTimestamp.ts (timestampState atom → useUIStore)
  - Modified: src/pages/Main/handlers/useArchiveMessage.tsx (chatsState Recoil → useChatsStore.getState())
  - Modified: src/pages/Main/handlers/useHandleDeleteChat.tsx (chatsState Recoil → useChatsStore.deleteChat)
  - useMessageStatus.tsx needed no changes — it only depends on useUpdateMessage which was rewired
- **Learnings for future iterations:**
  - For Recoil's functional `setChats(chats => ...)` updater pattern, Zustand equivalent is `useChatsStore.getState()` to read + `setChats(newValue)` to write
  - Inside useEffect callbacks and event handlers, use `useChatsStore.getState()` for fresh state reads (not stale closure values)
  - After this story, no files in src/hooks/ or src/pages/Main/handlers/ import from Recoil or atoms/ anymore
  - The chats store's `updateMessage`, `deleteChat`, and `setTyping` methods map cleanly to what the hooks needed
---

## 2026-03-29 - US-004c
- Removed Recoil completely — all remaining components rewired to Zustand
- Files changed:
  - Deleted: entire src/atoms/ directory (9 files: chats.ts, dialog.ts, effects/persist.ts, focus.ts, replyingTo.ts, sessionTimeout.ts, theme.ts, timestamp.ts, user.ts)
  - Modified: src/App.tsx (removed RecoilNexus, useRecoilCallback, useRecoilValue → useAuthStore), src/index.tsx (removed RecoilRoot)
  - Modified: src/components/Dialog.tsx (useRecoilState/useSetRecoilState → useUIStore)
  - Modified: src/pages/Main/index.tsx, Chat/ChatBody.tsx, Chat/UsersReaction.tsx, Chat/Message.tsx, Chat/MessageInput.tsx, Conversations/index.tsx, Settings/index.tsx, Settings/Sections/SessionTimeout.tsx, Settings/Sections/Theme.tsx, Settings/Sections/ManageData.tsx, SideHeader/SearchDialog.tsx, SideHeader/index.tsx
  - Modified: src/pages/Login/handlers/useHandleMnemonicSubmit.tsx (removed defaultChats import, inlined `{}`)
  - Modified: vite.config.ts (removed atoms alias)
  - Removed deps: recoil, recoil-nexus, recoil-persist
- **Learnings for future iterations:**
  - Recoil's `themeState` and `sessionTimeoutState` were selectors derived from user settings — Zustand equivalent is reading directly from `useAuthStore(s => s.user?.settings.theme)` and using `updateSettings()` to write
  - Recoil's `setDialog(dialog => {...dialog, ...})` functional updater pattern doesn't work with Zustand's plain setter — replace with direct value: `setDialog({...})`
  - When removing a path alias from vite.config.ts, no need to touch tsconfig.json if using baseUrl
  - `defaultChats` from atoms/chats.ts was just `{}` — can be inlined
---

## 2026-03-29 - US-005
- Redesigned login page using shadcn/ui + Tailwind CSS
- Files changed:
  - Rewritten: src/pages/Login/index.tsx — replaced Bootstrap Container/Row/Col/Form with shadcn Card, Input, Label, Button + Tailwind layout
  - Modified: src/pages/Login/handlers/useHandleRecovery.tsx — replaced react-bootstrap Form/Form.Control with shadcn Input/Label + Tailwind
  - Modified: src/pages/Login/handlers/useHandleMnemonicSubmit.tsx — replaced react-bootstrap Form.Control with shadcn Input/Label
  - Modified: src/pages/Login/handlers/useHandleRegenerate.tsx — replaced react-bootstrap Form/Form.Control with shadcn Input/Label + Tailwind
  - Rewritten: src/components/Dialog.tsx — replaced Bootstrap Modal with shadcn Dialog, kept DialogClose export for backward compat
  - Created: src/components/ui/card.tsx, dialog.tsx, input.tsx, label.tsx (via shadcn CLI)
- Added features: password show/hide toggle with lucide Eye/EyeOff icons, Loader2 spinner on submit
- **Learnings for future iterations:**
  - shadcn Dialog uses radix-ui primitives — wraps with `Dialog > DialogContent > DialogHeader/Footer`
  - The app's Dialog system (`UIStore.dialog`) is separate from shadcn's Dialog component — the app Dialog component bridges the two
  - `DialogClose` from `components/Dialog` is still used by old Main page components and must be kept until those are redesigned
  - When adding shadcn components, no package.json changes needed if radix-ui deps were already installed from a previous component (e.g. Button)
  - Recovery dialog Content components now use plain `<div>` instead of `<Form>` since form submission is handled by the Dialog's onConfirm button
---

## 2026-03-29 - US-006
- Redesigned register page using shadcn/ui + Tailwind CSS
- Files changed:
  - Rewritten: src/pages/Register/index.tsx — replaced Bootstrap Container/Row/Col/Form with shadcn Card, Input, Label, Button + Tailwind layout; added nick availability check on blur with inline error, loading spinner
  - Rewritten: src/pages/Register/SeedDialog.tsx — replaced Bootstrap Alert/Container/Row/Col with Tailwind grid (4 cols x 6 rows), destructive alert banner for warning
- **Learnings for future iterations:**
  - SeedDialog uses a 4-column grid with `grid-cols-4 gap-2` to display 24 words (6 rows)
  - The Dialog component's title uses `submitLabel` to determine header text — "I have saved my seed phrase" maps to the default "Confirm" title
  - Nick availability check on blur uses the existing `GET /users?nick=X&exact=true` endpoint — 200 means taken, error means available
  - Registration flow is unchanged: submit → check nick → show mnemonic dialog → onConfirm generates keys → API post → navigate
---

## 2026-03-29 - US-007
- Built responsive two-column layout shell with Tailwind CSS
- Files changed:
  - Rewritten: src/pages/Main/index.tsx — replaced Bootstrap Container/Row/Col with Tailwind flex layout + shadcn ScrollArea for sidebar
  - Rewritten: src/pages/Main/SideHeader/index.tsx — replaced Bootstrap Button/Plus with "diskreta." branding + lucide MessageSquarePlus + shadcn Button
  - Modified: src/pages/Main/Settings/index.tsx — replaced react-bootstrap-icons GearWideConnected with lucide Settings + shadcn Button
  - Modified: src/styles/globals.css — added #root { height: 100% } for full-height layout
  - Created: src/components/ui/scroll-area.tsx (via shadcn CLI)
- Layout features:
  - Desktop (>=768px): sidebar (w-80) + main panel side by side with border separator
  - Mobile (<768px): one panel at a time, toggled via `hidden`/`flex` classes based on activeChatId
  - Empty state with MessageSquare icon + "Select a conversation" when no chat active
  - SideHeader shows "diskreta." branding with new-chat and settings icon buttons
- **Learnings for future iterations:**
  - Tailwind responsive `hidden md:flex` / `flex` pattern replaces CSS media query show/hide logic cleanly
  - shadcn ScrollArea wraps radix-ui ScrollArea — good for sidebar scrollable content
  - Settings component is self-contained (renders its own trigger button) — just include it in SideHeader
  - #root needs explicit height: 100% since body is position: fixed with inset: 0
---

## 2026-03-29 - US-008
- Built conversations list with Tailwind + shadcn components
- Files changed:
  - Rewritten: src/pages/Main/Conversations/index.tsx — replaced Bootstrap ListGroup/react-bootstrap-icons with shadcn Avatar, Badge, AlertDialog + Tailwind layout
  - Created: src/components/ui/avatar.tsx, src/components/ui/badge.tsx, src/components/ui/alert-dialog.tsx (via shadcn CLI)
- Features:
  - Sorted by latest message timestamp (newest first)
  - Each row: avatar with initials, recipient nicks, last message preview (or typing indicator), relative timestamp
  - Unread badge with count
  - Active chat highlighted with `bg-accent`
  - Delete button on hover (desktop) with AlertDialog confirmation
  - "You:" prefix for own messages in preview, photo emoji for media-only messages
- **Learnings for future iterations:**
  - shadcn AlertDialog is better than window.confirm for delete confirmations — uses radix-ui AlertDialog primitives
  - `group` + `group-hover:flex` Tailwind pattern works well for revealing action buttons on hover
  - The old `useHandleDeleteChat` handler is still used by ChatHeader (US-010) — keep it until that's redesigned
  - Avatar initials: first 2 characters of nick uppercased is a simple fallback
---
